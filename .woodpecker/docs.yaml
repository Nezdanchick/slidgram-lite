# DO NOT EDIT
# Generated from .copier-answers.yml

when:
  event: [push, tag, pull_request]
  path: ["slidgram/**/*.py", "docs/**/*", "uv.lock", ".woodpecker/docs.yaml"]

steps:
  build:
    image: codeberg.org/${CI_REPO_OWNER}/slidgram:ci
    commands:
      - uv sync --all-groups --all-extras
      - cd docs && make html

  publish:
    image: codeberg.org/slidge/woodpecker-publish-pages
    when:
      event: [push, tag]
    settings:
      token:
        from_secret: CODEBERG_TOKEN
